/** Reported by /api/health. Bump with package.json AND the VERSION file — this
 * number is the only way to tell a landed deploy from a stale one (see
 * docs/DEPLOYMENT.md), so it is useless if the three ever disagree. */
export const APP_VERSION = "1.2.1";
